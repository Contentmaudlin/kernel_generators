CXX = g++
CXXFLAGS = -g -Wall -std=c++17
CC = g++

TARGETS = list vector move ptr

default: $(TARGETS)

vector:
vector.o:

list:
list.o:

move:
move.o:

ptr:
ptr.o:

clean:
	rm -rf $(TARGETS) a.out *.o
