CXX = g++
CXXFLAGS = -g -Wall -std=c++17
CC = g++

TARGETS = list vector move ptr impl1

default: $(TARGETS)

vector:
vector.o:

list:
list.o:

move:
move.o:

ptr:
ptr.o:

nested:
nested.o:

impl1:
impl1.o:

clean:
	rm -rf $(TARGETS) a.out *.o
