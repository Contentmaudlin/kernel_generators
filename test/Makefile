CXX = g++
CXXFLAGS = -g -Wall -std=c++17 $(INCLUDE)
CC = g++
SOURCEDIR=../src
INCLUDE=-I $(SOURCEDIR)

TARGETS = main_tester

default: $(TARGETS)

main_tester:

main_tester.o:

clean:
	rm -rf $(TARGETS) a.out *.o
